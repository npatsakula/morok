---
sidebar_label: Драйвер AM
---

# Драйвер AM (Userspace)

Драйвер **AM** — это второй [`AmdIface`](./overview.md)-бэкенд, который
управляет PCI-BAR'ами GPU напрямую, полностью обходя ядерный драйвер
`amdgpu`/KFD. Это порт userspace-драйвера AM из tinygrad. Мотивация конкретна:
на чипах gfx11+ с одним XCC безблокировочный путь
[многоочередной диспетчеризации](./queues-and-dispatch.md) способен загнать
микродвижки CP в ожидания, которые прошивка MES ядра не умеет вытеснять, и
заклинить её вплоть до невосстановимого сброса. Общее слабое место — планировщик
ядра: именно отказ этого класса и заставляет пул полос оставаться консервативным
на любом железе. Если мы сами владеем GPU — таблицами страниц, прошивкой,
планированием — ядро никогда не оказывается на пути диспетчеризации и заклинить
его нечем.

:::caution[В разработке — пока нельзя выбрать]
Эта страница описывает и то, что существует сегодня, и дорожную карту для
остального. **`SVOD_AMD_BACKEND=am` сейчас возвращает ошибку** (`device.rs`
принимает только `kfd`): пока ни один AM-тип не реализует шов
[`AmdIface`](./overview.md), поэтому доступная сегодня инициализация
выполняется только через примеры `am_*`, а не через `AmdDevice`. То, что уже
есть, проверено **вплоть до передачи управления движку**; пока не удалось
подтвердить, что хоть один движок GPU выполняет работу на целевом железе (см.
[границу VF](#the-vf-boundary)). Разделы ниже явно отмечают статус каждой части.
:::

Код находится в `device/src/amd/am/`. Он компилируется на **каждом
Unix-хосте** (`cfg(unix)`, как и остальной бэкенд — см. [модель провайдера,
определяемого во время выполнения](./overview.md)), поэтому всегда проходит
проверку типов, линтинг и unit-тесты — бэкенд выбирается во *время выполнения*,
а не прячется за cargo-фичей, которая могла бы протухнуть.

---

## Целевое железо: SR-IOV VF на CDNA3 (gfx9.4.3)

Драйвер нацелен на GPU архитектуры **CDNA3** — **gfx9.4.3**, 8 XCC в
режиме SPX — и конкретно на его разновидность **SR-IOV Virtual Function (VF)**
(GPU представляет собой VF, проброшенную в гостевую KVM-систему). `AmDev::open`
сразу отклоняет всё остальное: на не-VF-функции или на версии GC, у которой
major.minor отличаются от `(9, 4)`, инициализация немедленно завершается
ошибкой (`device/src/amd/am/dev.rs`). gfx1151 (RDNA3.5) больше не является
*целью*, но арх-ветка gfx11 остаётся реализованной и покрытой unit-тестами — а
её геометрия таблицы страниц и хелперы диапазонов palloc — это ровно то, что
переиспользует путь gfx9.

> Железо, на котором велась инициализация: SR-IOV VF от AMD Instinct MI300X
> (gfx942 / GC 9.4.3). Ничего другого `AmDev::open` не принимает.

Статус **VF** (а не bare metal) — определяющее ограничение, и именно оно задаёт
всю архитектуру драйвера:

- **GC MMIO закрыт хостом (host-gated).** Любое *прямое* чтение GC-регистра
  возвращает `0xffffffff`. Весь доступ к регистрам GC / GCVM должен идти
  **косвенно через RLC** (путь RLCG): подготовить значение в RLC scratch,
  инициировать запись через `RLC_SPARE_INT`, дождаться завершения.
- **VRAM/discovery недоступны, пока доступ не выдан.** Фреймбуфер (а значит и
  таблица IP discovery) недоступен для чтения, пока хостовый **GIM**
  (хост-драйвер SR-IOV) не предоставит доступ через **mailbox-рукопожатие** —
  поэтому оно выполняется *перед* discovery.
- **Хостовая PF владеет привилегированными подсистемами:** PSP, SMU,
  тактирование, прошивка / world-switch, конфигурация L2-кэша, системная
  апертура и — что критично — **маршрутизация doorbell-апертуры**. AM
  программирует только то per-VF-состояние, которое гостю позволено трогать
  (page-table context0, диапазоны инвалидации на движок, TLB-флаши,
  ring/queue MQD), ровно так же, как ядерный код IP `*_v*` пропускает эти блоки
  под `amdgpu_sriov_vf`.

Здесь всё ровно наоборот по сравнению с AM из tinygrad, который работает
**только на bare metal** (он отвязывает `amdgpu` и владеет всем устройством).
VF-разновидность — это другой драйвер: mailbox + косвенный доступ к регистрам
через RLCG + программирование только per-VF-хабов.

---

## Что существует сегодня

Всё **компилируется и покрыто unit-тестами без GPU** там, где это чистая логика;
части, работающие с железом, дополнительно проверены на живой VF через программы
`device/examples/am_*.rs`. Таблицы страниц опираются на внедряемый трейт
`PhysMem` (обычный буфер в тестах; в реальном драйвере — VRAM, отображённая
через BAR).

| Группа | Модуль(и) | Что делает | Статус |
|---|---|---|---|
| **Discovery** | `pci.rs`, `discovery.rs` | sysfs-mmap BAR'ов (BAR0 VRAM / BAR2 doorbell / BAR5 MMIO), r/w конфигурационного пространства, парсер IP discovery с проверкой границ (per-XCC базы сегментов, `gc_info` v1/v2) | **Проверено на HW**; парсер discovery покрыт unit-тестами |
| **Доступ к регистрам** | `regaccess.rs`, `rlcg.rs`, `mailbox.rs`, `regs.rs`, `regs_gen.rs` | mailbox-рукопожатие mxgpu VF↔GIM, косвенный r/w GC/GCVM через RLCG (per-XCC), роутер MMIO/RLCG, вендоренные таблицы регистров | **Проверено на HW**; логика выбора и кодирования в таблицах регистров покрыта unit-тестами |
| **Память (GMMU)** | `mm/{tlsf,pagetable,manager,mod}.rs` | TLSF-аллокаторы VA/PA/таблиц страниц, 4-уровневый / 48-битный обход, кодирование PTE/PDE для gfx9 **и** gfx11, выбор huge-page, освобождение таблиц, `valloc`/`vfree` | **Готово** + тесты (путь записи PTE отработан на HW) |
| **Инициализация GMC** | `ip/gmc.rs` | программирование context0 обоих хабов (start/end/base + CNTL), включение MX_L1_TLB, диапазоны инвалидации на движок, TLB-флаш ENG17, HDP-флаш, декод статуса сбоя | **Проверено на HW** до уровня программирования context |
| **Инициализация GFX** | `ip/gfx.rs` | включение MEC (инвалидация icache, golden `GB_ADDR_CONFIG`, диапазон doorbell, unhalt), сборка compute MQD v9, активация HQD (`CP_HQD_ACTIVE=1`), PM4 `WRITE_DATA` | **MEC HQD активируется**; очередь пока ничего не исполняет |
| **Инициализация SDMA** | `ip/sdma.rs` | unhalt F32, программирование RB base/rptr/wptr + doorbell, submit + `wait_idle` | **ring запрограммирован**; движок пока не потребляет |
| **Оркестратор** | `dev.rs` | `AmDev::open` = mailbox → discovery → GMMU → GMC context0 → flush; `valloc`, `vram_read/write`, `release` | **Проверено на HW** до GMC |

### GMMU и gfx9

Геометрия таблицы страниц — **4-уровневая / 48-битная**
(`va_shifts = [12, 21, 30, 39]`), форма, **общая для gfx9/11/12** — так что сама
геометрия не ветвится по архитектуре. От архитектуры зависит только листовое
кодирование PTE (особенно поле типа памяти MTYPE), и **теперь реализованы и
покрыты unit-тестами обе архитектуры — gfx9 (CDNA) и gfx11 (RDNA3)**: gfx9
кладёт MTYPE в биты 57–58, выставляет `bfs` у табличных записей PDB1 и бит
translate-further у табличных записей PDB0, а листья PDB1/PDB2 помечает
`PDE_PTE` (лист PDB0 на 2 МиБ — это, наоборот, *отсутствие* translate-further).
**gfx12 — единственный оставшийся `unimplemented!`** (константы зафиксированы,
но ещё не проверены на железе; тест проверяет, что код паникует). `MemoryManager`
ведёт три
TLSF-субаллокатора (пространство VA, физическая VRAM, пул таблиц страниц) и
обходит таблицу в режимах `Inspect` / `Create` / `Free`, освобождая пустые
таблицы при unmap.

### Таблицы регистров генерируются один раз, затем вендорятся

tinygrad — это иногда отсутствующий сабмодуль, поэтому сборка никогда не должна
от него зависеть. Вместо этого `device/tools/gen_am_regs.py` запускается
**вручную** при добавлении или обновлении архитектуры: он парсит
`autogen/am/regs.py` из tinygrad и выдаёт закоммиченный `am/regs_gen.rs`.
`regs.rs` просто подключает его через `include!`. При загрузке нужная таблица
выбирается по обнаруженному `ip_ver` (`select` берёт наибольшую версию
`≤ ip_ver` с тем же major — правило `import_module` из tinygrad). Закоммиченные
таблицы теперь покрывают и набор gfx9.4.3/CDNA3 (`gc 9.4.3`, `mmhub 1.8.0`,
`osssys 4.4.2`, `sdma 4.4.2`, `nbio 7.9.0`, `hdp 4.4.2`, `mp 11.0.0`/`13.0.0`), и
набор gfx11.5.0. Чтобы добавить архитектуру, достаточно расширить список модулей
в генераторе и перезапустить его — без изменений в логике сборки или рантайма.

---

## Граница VF {#the-vf-boundary}

Это та стена, перед которой сейчас останавливается инициализация. Гость может
**запрограммировать** движки, но не может ими **управлять**, потому что
doorbell-апертура, доставляющая write-pointer кольца до командного процессора,
**принадлежит PF**. Её включение из VF (запись в `_PF`-регистры доступа к
doorbell в блоке BIF) заклинивает mailbox VF↔GIM и требует полной перезагрузки
VM — поэтому `enable_doorbell_aperture` в `ip/gfx.rs` есть, но явно помечен как
**не вызывать на VF**.

Конкретные последствия, оба воспроизводятся примерами:

- **Compute-очередь MEC активируется, но не исполняет** (`am_compute`): HQD
  сообщает `CP_HQD_ACTIVE = 1`, но пакет `WRITE_DATA` так и не записывает свой
  sentinel в VRAM — CP не видит doorbell.
- **SDMA-ring запрограммирован, но не потребляет** (`am_sdma`): read-pointer
  остаётся заклиненным; сбои обхода таблицы страниц MM-хаба всё ещё закрыты.

Так что сегодня AM **проверен на HW вплоть до передачи управления движку** —
discovery, владение, GMMU и GMC доказаны на живой VF — а KFD остаётся рабочим
VF-бэкендом. Именно преодолению этой границы и посвящены оставшиеся вехи.

---

## Что работает на железе сегодня

Каждый пример `am_*` — это самостоятельный оракул инициализации, который
прогоняют на живой VF:

| Пример | Что доказывает | Статус |
|---|---|---|
| `am_discovery` | mmap BAR'ов + IP discovery (8× GC 9.4.3, SDMA, AID), только чтение — сосуществует с привязанным `amdgpu` | **работает** |
| `am_own` | mailbox-grant + RLCG scratch echo + не-закрытый `GRBM_STATUS` на всех 8 XCC | **работает** |
| `am_gmc` | context0 GC + MM запрограммированы; ACK TLB-флаша ENG17 на всех 8 XCC; ни одной защёлкнутой protection fault | **работает** |
| `am_sdma` | настройка SDMA-ring + submit | ring запрограммирован, **движок не потребляет** |
| `am_compute` | включение MEC + активация MQD + `WRITE_DATA` | **HQD активируется**, очередь не исполняет |

---

## Что ещё отложено

Привилегированные подсистемы, принадлежащие PF, **отсутствуют в дереве** — на VF
ими владеет GIM, и гостю с ними делать нечего; на bare metal это последний,
самый рискованный порт:

- **Загрузка прошивки PSP** — рукопожатие загрузчика sOS / TMR / загрузка
  прошивки на каждый IP. На VF принадлежит GIM.
- **SMU / тактирование** — управление питанием и тактовыми частотами. На VF
  принадлежит GIM.
- **Обработчик прерываний (IH)** — `ip/ih.rs` не существует; таблица регистров
  OSSSYS вендорена, но не используется. Инициализация работает через опрос, а не
  через обработку прерываний.
- **Реализация шва `AmIface`** — пока ни один AM-тип не реализует
  [`AmdIface`](./overview.md), поэтому AM нельзя выбрать как бэкенд устройства;
  `AmDev` достижим только через примеры.

---

## Дорожная карта

Работа разбита на вехи, каждая из которых тестируется независимо на живой VF (а
для блоков, которыми владеет PF, — сверяясь с bare-metal-версией AM из tinygrad
как с эталоном). Ранние вехи реализованы; полная сквозная интеграция AM — задача
на будущее.

Как только движок начнёт потреблять работу, а шов будет подключён,
AM можно будет выбрать через `SVOD_AMD_BACKEND=am`, и он запустит всю
существующую верхнюю половину без изменений. Параллельная нагрузка, которая
вызывала краши и ради которой затевался AM, к тому моменту уже не сможет
обрушить ядро — оно обойдено.

---

## Почему это важно

Драйвер AM — это настоящий ответ на проблему заклинивания прошивки, которую
зажатие пула полос ([`SVOD_AMD_HW_QUEUES=1`](./queues-and-dispatch.md)) лишь
обходит. Дорогие, не требующие GPU части — GMMU, таблицы регистров, механизмы
косвенного доступа через mailbox/RLCG — построены и проверены на живой VF, а
таблицы страниц, GMC и
рукопожатие владения уже работают. Оставшийся разрыв — это аппаратная граница
(doorbell-апертура, принадлежащая PF), а не вопрос дизайна. И поскольку AM
подключается за тем же [швом](./overview.md) — пять обязательных методов плюс
три хука с реализацией по умолчанию, — ничего из машинерии диспетчеризации,
компиляции или графов менять не придётся, когда он будет готов.
